# הוראות קבועות לעבודה בפרויקט Audiobooks — גרסת מסמך 1.7.0

## מקור אמת וסדר עדיפויות

1. `MASTER_SPEC.md` — מטרת האתר והמבנה הקבוע.
2. בקשת המשתמש הנוכחית.
3. `DECISIONS.md` — החלטות שכבר התקבלו.
4. `QUALITY_GATES.md` — תנאי הסף לפני פרסום.
5. קובצי ה־HTML והקוד הקיימים.

במקרה של סתירה אין למחוק או להחליף עבודה בשקט. יש לתעד את הסתירה ב־`REVIEW_PACKET.md`, לבחור בפתרון השמרני שאינו ממציא מידע, ולהמשיך ככל האפשר.

## מבנה דפי HTML

- כל ספרייה זו היא מאגר Git עצמאי שמפורסם ב־GitHub Pages.
- האתר נשאר אתר Audiobooks כללי אחד בשורש המאגר; קטגוריות עתידיות מתווספות בתוך אותו דף/אתר ולא כאתרים או תיקיות נושא נפרדים.
- דף הבית הוא `index.html` בשורש. ברירת המחדל היא One Page: ה־HTML, ה־CSS וה־JavaScript של הדף נשמרים יחד, אלא אם יש סיבה מתועדת להפריד נכסים.
- כל קובץ HTML נוסף בשורש יפורסם אוטומטית בכתובת ששמו מופיע בה.
- לכל ספר חייב להיות ז׳אנר ראשי (`genre`) נפרד מנושא פנימי (`topic`). ז׳אנרים כגון Self-Help, מתח ומדע בדיוני הם עמיתים בני אותה רמה ואינם תתי־קטגוריות זה של זה.
- כל 194 הרשומות הקיימות שייכות כרגע ל־`Self-Help`. ספר עתידי מז׳אנר אחר חייב לקבל `genre` מפורש; אין לסווג אותו כנושא של Self-Help.
- הניווט הראשי נבנה אוטומטית מערכי `genre`, ומסנן הנושא נבנה מתוך `topic` של הז׳אנר שנבחר.

## מיון ואמינות נתונים

- אפשרויות המיון הקבועות כוללות חדש ביותר לפי שנת פרסום, ציון Audible, מספר דירוגי Audible, שם ספר ומחבר; ערכים חסרים תמיד מופיעים אחרי ערכים זמינים.
- יש להציג ליד המיון את מספר הרשומות שעבורן הנתון הנבחר זמין, כדי לא ליצור רושם שכל הקטלוג מכוסה.
- אין להציג מיון מכירות על סמך השדה הישן `sales`: הוא חלקי, מתייחס למכירות עותקים כלליות ואינו נתון מכירות Audible אחיד.
- מיון מכירות Audible יכול להופיע רק כאשר לכל ספר יש ערך מספרי `audible_sales`, דגל `audible_sales_verified: true`, כתובת מקור HTTPS ותאריך אימות מלא בפורמט YYYY-MM-DD.

## יבוא מועמדים

- רשומת מועמד תפורסם כאודיובוק רק לאחר התאמת כותרת ומחבר ל־ASIN רשמי בקטלוג Audible ואימות שהמוצר ניתן להאזנה.
- אין להשלים ASIN, קישור, קריין, תאריך, תקציר, דירוגים או מכירות באמצעות ניחוש. שדה שלא אומת נשאר חסר ומוצג כך.
- לפני יבוא יש לנרמל כותרת ומחבר ולסנן כפילויות בתוך המקור ומול כל הקטלוג.
- קישור Audible ישיר חייב להיות HTTPS, להתאים ל־ASIN המאומת ולשמור תאריך אימות.

## תקצירים והעמקה

- קישור העמקה מוצג רק מתוך מערך `resources` של הספר ולא מתוך URL משוער או חיפוש כללי.
- כל משאב חייב לכלול `type`, `title`, `source`, `url`, `verified: true` ו־`verified_at`. מומלץ להוסיף גם `language` ו־`duration_seconds`.
- משאב `youtube_long` חייב להפנות ל־HTTPS, לא לנתיב `/shorts/`, ולהיות באורך 1,200 שניות לפחות.
- אין להציג iframe, נגן מוטמע או תמונה מרוחקת של צד שלישי. יש להשתמש בקישור חיצוני עם `noopener noreferrer` כדי לשמור על פרטיות האתר.
- כאשר אין מקור ארוך ומאומת, יש להציג זאת במפורש ולא להמציא קישור.
- דפים בעברית חייבים לכלול `<html lang="he" dir="rtl">`, קידוד UTF-8, `<title>` ותג viewport.
- יש להשתמש בנתיבים יחסיים המתאימים לתת־נתיב של GitHub Pages. אין להשתמש ב־`file://`, בנתיבי Windows מקומיים או בקישורים שתלויים ב־localhost.
- אין להכניס נתוני דמה, קישורים מומצאים, Lorem Ipsum או עובדות שלא אומתו.
- אין למחוק או להחליף תוכן קיים בלי צורך ברור ותיעוד.
- יש לשמור UTF-8 בכל קובצי הטקסט.

## פרטיות, מעקב ואינדוקס

- האתר מיועד לגישה באמצעות קישור ישיר בלבד ואינו מיועד לקידום או לאינדוקס במנועי חיפוש.
- כל דף HTML חייב לכלול `<meta name="robots" content="noindex,nofollow,noarchive,nosnippet,noimageindex">`.
- `robots.txt` חייב לחסום את כל הסורקים באמצעות `User-agent: *` ו־`Disallow: /`.
- אין ליצור sitemap, להזין את האתר לכלי מנהלי אתרים או לפרסם קישורי קידום.
- אין להוסיף Google Analytics, pixels, trackers, telemetry או סקריפטים של צד שלישי לצורכי מדידת מבקרים.
- אין לשמור כתובת IP, מזהה מכשיר, cookies של מעקב או פרטי ביקור.
- הגדרות אלה אינן בקרת גישה: מי שמקבל את הקישור יכול לפתוח ולשתף אותו.

## GitHub ופרסום

- הענף הראשי הוא `main` וה־remote הקבוע הוא `origin`.
- כל push אל `main` מפעיל בדיקות ולאחר הצלחתן מפרסם את האתר דרך GitHub Actions ו־GitHub Pages.
- אין להעלות את תיקיית `_site`; היא תוצר זמני ונמצאת ב־`.gitignore`.
- אין לבצע force push, למחוק היסטוריה או לשנות קבצים מחוץ לתיקיית הפרויקט.
- לפני commit יש להריץ `node tools/validate-site.mjs` ו־`node tools/build-site.mjs`.
- לאחר שינוי תוכן, יש לבצע commit ברור ולדחוף ל־`origin/main` רק כאשר הבדיקות עוברות.

## בקרת גרסאות ותיעוד

- מסמכי התהליך חייבים להציג בכותרת הראשית מספר גרסת מסמך גלוי.
- בשינוי מהותי יש לעדכן את `PROJECT_STATUS.md`, את `REVIEW_PACKET.md`, ואם נדרשת החלטה חדשה גם את `DECISIONS.md`.
- אין להכניס secrets, אסימוני גישה או פרטי התחברות למאגר.

## דיווח חובה בסיום סבב

`REVIEW_PACKET.md` יתעד:

1. מה הושלם.
2. אילו קבצים נוצרו או שונו.
3. אילו בדיקות הורצו ומה היו תוצאותיהן.
4. בעיות, מגבלות וסיכונים.
5. מה נשאר והאם שער האיכות עבר.
