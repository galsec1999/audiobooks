# מפרט־על לאתר Audiobooks — גרסת מסמך 1.1.0

## מטרה

לפרסם אוסף דפי One Page בנושא ספרי שמע מתוך מאגר GitHub אחד, בכתובת GitHub Pages יציבה שנגישה באמצעות קישור ישיר, ללא מעקב מבקרים וללא אינדוקס מכוון במנועי חיפוש.

## מבנה הפרסום

- קובץ הבית מוגדר בשדה `homepage` של `site.config.json` ומפורסם כ־`index.html`.
- כל קובץ `*.html` נוסף בשורש הפרויקט נכלל בפרסום ונשאר נגיש בשם הקובץ המקורי.
- קובצי המקור נשארים בשמות תיאוריים; תיקיית `_site` נוצרת רק לצורך בדיקה ופרסום.
- האתר הוא סטטי ואינו דורש שרת יישום או בסיס נתונים.

## דרישות קבועות

- עברית ו־RTL בדפים עבריים.
- תצוגה תקינה במחשב ובטלפון.
- UTF-8 ונתיבים בטוחים ל־GitHub Pages.
- בדיקה אוטומטית בכל push ובכל pull request ל־`main`.
- פרסום אוטומטי רק לאחר שכל הבדיקות הצליחו.
- `noindex,nofollow` בכל דף ו־`robots.txt` החוסם את כל הסורקים.
- ללא analytics, trackers, sitemap או קישורי קידום.
