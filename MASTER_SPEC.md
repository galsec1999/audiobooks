# מפרט־על לאתר Audiobooks — גרסת מסמך 2.1.0

## מטרה

לפרסם אתר One Page כללי אחד בנושאי ספרי שמע מתוך מאגר GitHub בשם `audiobooks`, ללא מעקב מבקרים וללא אינדוקס מכוון במנועי חיפוש. קטגוריות נוספות יתווספו בתוך אותו אתר בעתיד.

## מבנה הפרסום

- קובץ הבית הוא `index.html` בשורש המאגר ומוגדר בשדה `homepage` של `site.config.json`.
- כל קובץ `*.html` נוסף בשורש נכלל בפרסום ונשאר נגיש בשם הקובץ המקורי.
- אין ליצור תיקיית נושא או אתר נפרד עבור קטגוריה חדשה; יש להרחיב את האתר הכללי.
- מודל הספר מפריד בין `genre` ראשי לבין `topic` פנימי. הניווט הראשי מציג ז׳אנרים עמיתים ומסנן הנושא תלוי בז׳אנר הנבחר.
- הקטלוג כולל 1,179 ספרים: 759 ספרי Self-Help, ‏210 ספרי מדע בדיוני ו־210 ספרי מתח. שני הז׳אנרים החדשים מוגבלים לחלון 29.08.2019–29.08.2026, לציון Audible ‏4.3 ומעלה ולפחות 100 דירוגים. בכל ז׳אנר יש בדיוק 70 סדרות Audible; תוספות הסדרות ל־Self-Help עומדות בציון 4.3 ולפחות 25 דירוגים.
- הנושא `AI ועסקים` כולל 30 ספרים מאומתים. חמש התוספות האחרונות עומדות בשער עדכניות של שלוש שנים ובשער איכות של ציון 4.4 ומאה דירוגי Audible לפחות; שער הזמן אינו חל על נושאים אחרים.
- רשומה חדשה נחשבת אודיובוק מאומת רק לאחר התאמה לקטלוג Audible הרשמי; שדות שלא אומתו נשארים חסרים.
- כל כרטיס כולל אזור “תקצירים והעמקה”. האזור מציג רק משאבים ישירים ומאומתים; מצב ללא משאב הוא תוצאה תקינה.
- לכל 1,179 הספרים יש תקציר בעברית. ל־994 הרשומות בעלות תמצית Audible/מו״ל באנגלית מוצג תרגום עברי מקומי, לצד קישור המקור, תאריך אימות וסימון מפורש של התרגום.
- תצוגת ספרים ותצוגת סדרות זמינות זו לצד זו; ספרי סדרה מקובצים לפי מטא־דאטה Audible, כוללים עטיפה ומסודרים לפי מספרם בסדרה.
- Audible מוצג כקישור ישיר כאשר הזהות אומתה. Spotify, Google Play Books, Kobo ו־Libro.fm מוצגים כקישורי חיפוש בלבד, עם הודעה שהזמינות תלויה באזור ובמהדורה. Apple Books אינו נכלל לפי החלטת המשתמש.
- האתר ניתן להתקנה כ־PWA באמצעות manifest, ‏Service Worker ואייקונים מקומיים; המטמון מוגבל למעטפת האתר מאותו origin ואינו אוסף נתוני שימוש.
- בטלפון המסננים סגורים כברירת מחדל בתוך מגירה נגישה שניתן לפתוח ולסגור, כדי שכרטיסי הספרים יישארו גלויים.
- הממשק מאפשר מיון לפי חדש ביותר, ציון Audible ומספר דירוגי Audible ומציג את היקף זמינות הנתון. ספרים ללא הנתון מופיעים בסוף.
- מיון לפי מכירות Audible אינו מוצג עד שקיים לכל הקטלוג מערך מספרי, מלא ומאומת עם מקור ותאריך אימות.
- סרטון מאומת באזור ההעמקה חייב להיות ארוך מ־20 דקות ואינו יכול להיות Short.
- קובצי המקור נשארים בשמות תיאוריים; תיקיית `_site` נוצרת רק לצורך בדיקה ופרסום.
- האתר הוא סטטי ואינו דורש שרת יישום או בסיס נתונים.

## דרישות קבועות

- עברית ו־RTL בדפים עבריים.
- תצוגה תקינה במחשב ובטלפון.
- UTF-8 ונתיבים בטוחים ל־GitHub Pages.
- בדיקה אוטומטית בכל push ובכל pull request ל־`main`.
- פרסום אוטומטי רק לאחר שכל הבדיקות הצליחו.
- `noindex,nofollow` בכל דף ו־`robots.txt` החוסם את כל הסורקים.
- ללא analytics, trackers, sitemap או קישורי קידום.
